spring:
  config:
    activate:
      on-profile: client

client:
  serverHost: ${CLIENT_SERVER_HOST:82.156.228.71}
  serverPort: ${CLIENT_SERVER_PORT:9000}

node:
  device-id: ${NODE_DEVICE_ID:}
  auto-connect: ${NODE_AUTO_CONNECT:true}

server:
  address: ${CLIENT_HTTP_ADDRESS:127.0.0.1}
  port: ${CLIENT_HTTP_PORT:8081}
  tcp:
    enabled: false

transfer:
  receive-dir: ${TRANSFER_RECEIVE_DIR:downloads-client-1}
