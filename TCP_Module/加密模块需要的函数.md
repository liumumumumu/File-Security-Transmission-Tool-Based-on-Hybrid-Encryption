加密模块需要的函数



生成私钥()

生成公钥()

生成签名(challenge, 私钥)

验证签名(公钥，challenge, 签名) 返回布尔值

生成AES密钥()

加密AES密钥(AES密钥, 接收方公钥) 返回加密后的AES密钥

解密AES密钥(加密的AES密钥, 接收方的私钥)

加密数据块(数据块, AES密钥) 返回一个AesGcm数据块对象

解密数据块(数据库密文, nonce, tag,  AES密钥) 返回数据块明文







AesGcm数据块{

​	byte[] nonce; 加密用的随机数

​	byte[] ciphertext;密文

​	byte[] tag;用来验证数据没有被篡改

}